{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cp -r .next/static .next/standalone/.next/static 2>/dev/null || true && cp -r public .next/standalone/public 2>/dev/null || true && PORT=${PORT:-3030} HOSTNAME=0.0.0.0 node .next/standalone/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

